// Space Browser Component Logic

document.addEventListener('livewire:initialized', () => {
    // Logic to handle frontend-only interactions if needed
    // For now, Livewire handles the state, but we can add smooth scrolling or specialized interactions here.
    console.log('SpaceBrowser initialized');
});
